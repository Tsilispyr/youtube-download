services:
  yt-mp3:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./:/app
    # BGU_POT_SERVER_HOST is already set to localhost:4416 in the Dockerfile.
    # start.sh launches the token server and Flask together in one container.
    restart: unless-stopped
