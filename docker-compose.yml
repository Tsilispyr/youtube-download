services:
  yt-mp3:
    build: .
    ports:
      - "5000:5000"
    volumes:
      # Your MP3s will land here on your machine
      - ./downloads:/downloads
    environment:
      - DOWNLOAD_DIR=/downloads
    restart: unless-stopped
